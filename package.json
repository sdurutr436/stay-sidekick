{
  "name": "tfg-alberti",
  "version": "0.1.0",
  "private": true,
  "description": "Stay Sidekick — orquestador de entorno de desarrollo",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"11ty,angular\" --prefix-colors \"cyan,magenta\" \"npm run dev:web\" \"npm run dev:app\"",
    "dev:web": "npm --prefix web start",
    "dev:app": "npm --prefix frontend start",
    "install:all": "npm --prefix frontend install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
