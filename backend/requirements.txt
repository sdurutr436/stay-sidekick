# Framework
flask==3.1.3
python-dotenv==1.2.2

# CORS
flask-cors==6.0.4

# Validación de esquemas
marshmallow==4.3.0

# Sanitización HTML (reemplazo de bleach, basado en Rust/ammonia)
nh3==0.3.5

# Validación de email
email-validator==2.3.0

# Validación de teléfono (libphonenumber de Google)
phonenumbers==9.0.32

# Cliente HTTP (verificación Turnstile)
requests==2.34.2

# Rate limiting
flask-limiter==4.1.1
redis==8.0.0

# JWT (autenticación panel)
PyJWT==2.13.0

# Hashing de contraseñas
bcrypt==5.0.0

# ORM y base de datos
flask-sqlalchemy==3.1.1
flask-migrate==4.1.0
psycopg2-binary==2.9.12

# Servidor WSGI de producción
gunicorn==26.0.0

# Cifrado simétrico (Fernet — API keys en BD)
cryptography==48.0.0

# Parsing de Excel (.xlsx)
openpyxl==3.1.5
pandas==3.0.3

# Capa de abstracción para LLMs (Gemini, OpenAI, Anthropic, etc.)
litellm>=1.83.7

# Linter / formateador (usado en los checks de calidad)
ruff==0.15.16

# Cobertura de tests (usado en los checks de calidad)
pytest==9.0.3
pytest-cov==7.1.0
