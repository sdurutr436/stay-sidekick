# Framework
flask==3.1.1
python-dotenv==1.1.0

# CORS
flask-cors==5.0.1

# Validación de esquemas
marshmallow==3.26.1

# Sanitización HTML (reemplazo de bleach, basado en Rust/ammonia)
nh3==0.2.21

# Validación de email
email-validator==2.2.0

# Validación de teléfono (libphonenumber de Google)
phonenumbers==8.13.55

# Cliente HTTP (verificación Turnstile)
requests==2.32.3

# Rate limiting
flask-limiter==3.12

# JWT (autenticación panel)
PyJWT==2.9.0

# Hashing de contraseñas
bcrypt==5.0.0

# ORM y base de datos
flask-sqlalchemy==3.1.1
flask-migrate==4.1.0
psycopg2-binary==2.9.10

# Servidor WSGI de producción
gunicorn==21.2.0

# Cifrado simétrico (Fernet — API keys en BD)
cryptography==44.0.3

# Parsing de Excel (.xlsx)
openpyxl==3.1.5
pandas==2.2.3

# Capa de abstracción para LLMs (Gemini, OpenAI, Anthropic, etc.)
litellm>=1.40.0
