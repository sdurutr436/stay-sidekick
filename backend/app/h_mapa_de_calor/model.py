"""Mapa de calor — sin modelo propio.

Los datos procesados no se persisten en base de datos (cumplimiento RGPD).
La configuración (umbrales, columnas XLSX) se almacena en
empresas.configuracion["heatmap"] (JSONB existente).
"""
